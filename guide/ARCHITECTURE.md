# 🏗️ Архитектура UTM Cheating Simulator

## Структура проекта

```
Gameonpython/
├── main.py                 # Основной файл игры
├── requirements.txt        # Зависимости
├── README.md              # Описание игры
└── ARCHITECTURE.md        # Этот файл
```

## Компоненты игры

### 1. **GameState** (Enum)
Определяет состояния игры:
- `MAIN_MENU` - главное меню
- `GAME` - активная игра
- `GAME_OVER` - поимка преподавателем
- `WIN` - выполнение задачи успешно

### 2. **StudentActivity** (Enum)
Активности, которые студент может выполнять:
- `NORMAL` - учиться
- `CHEAT` - списывать
- `GAMES` - играть на телефоне
- `SLEEP` - спать
- `EAT` - есть

### 3. **Student** (Class)
Главный персонаж - студент.

**Атрибуты:**
- `x, y` - позиция на экране
- `current_activity` - текущая активность
- `score` - набранные очки

**Методы:**
- `draw()` - отрисовка студента
- `draw_activity_icon()` - отрисовка иконки активности

### 4. **Teacher** (Class)
Преподаватель, контролирующий студента.

**Атрибуты:**
- `x, y` - позиция на экране
- `looking_at_student` - смотрит ли в данный момент
- `look_timer` - таймер до следующего взгляда
- `look_duration` - продолжительность взгляда

**Методы:**
- `draw()` - отрисовка преподавателя с изменением выражения лица

### 5. **Button** (Class)
Интерактивная кнопка для действий.

**Атрибуты:**
- `rect` - прямоугольник для отрисовки и обнаружения кликов
- `text` - текст кнопки
- `action` - действие при нажатии
- `hovered` - состояние наведения мыши

**Методы:**
- `draw()` - отрисовка кнопки
- `is_clicked()` - проверка клика
- `update_hover()` - обновление состояния наведения

### 6. **Game** (Class)
Главный класс управления игрой.

**Атрибуты:**
- `state` - текущее состояние игры
- `student` - объект студента
- `teacher` - объект преподавателя
- `score` - текущий счёт
- `time_remaining` - оставшееся время
- `buttons` - список кнопок
- `messages` - список сообщений

**Ключевые методы:**
- `start_game()` - начало новой игры
- `schedule_teacher_actions()` - планирование действий учителя
- `draw_*()` - методы отрисовки разных экранов
- `handle_*_click()` - обработка кликов
- `update()` - обновление состояния
- `run()` - главный цикл игры

## Логика игры

### Основной цикл (run())

```
1. Обработка событий (клики, закрытие окна)
2. Обновление состояния (update())
3. Отрисовка (draw())
4. Ограничение FPS (60 кадров/сек)
```

### Система учителя

```
1. Учитель планирует случайный момент для взгляда
2. После задержки (2-5 сек) начинает смотреть
3. Смотрит в течение случайного времени (1-3 сек)
4. Если студент нажимает запрещённую кнопку - GAME OVER
5. После взгляда планируется следующий
```

### Обработка активностей

```
1. Если учитель смотрит на студента:
   - Нельзя нажимать на опасные кнопки (списать, играть, спать, есть)
   - Можно только учиться (NORMAL)
   
2. Если учитель отвлёкся:
   - Можно нажимать любую кнопку
   - Студент выполняет активность и получает очки
```

## Цвета и стили (УТМ тема)

- `UTM_PURPLE` = (102, 51, 153) - основной цвет УТМ
- `UTM_GOLD` = (255, 184, 28) - золотой цвет УТМ
- `SKIN` = (255, 220, 177) - цвет кожи персонажей
- `RED` = (220, 50, 50) - опасность (учитель смотрит)
- `GREEN` = (50, 200, 50) - безопасность (учитель отвлёкся)

## Система сообщений

Все действия сопровождаются сообщениями:
- Добавляются в список `self.messages`
- Каждое сообщение имеет время жизни (в кадрах)
- При каждом update() время уменьшается
- Сообщения удаляются по истечении времени

## Интеграция текстур (для будущего)

### Для добавления спрайтов:

1. Сохранить изображения в папку `assets/`
2. В методе `__init__` загрузить изображения:
```python
self.student_image = pygame.image.load("assets/student.png")
self.teacher_image = pygame.image.load("assets/teacher.png")
```

3. В методах `draw()` использовать `screen.blit()` вместо рисования фигур

### Для добавления звуков:

1. Сохранить звуки в папку `sounds/`
2. Использовать `pygame.mixer` для воспроизведения:
```python
pygame.mixer.init()
caught_sound = pygame.mixer.Sound("sounds/caught.wav")
caught_sound.play()
```

## Возможные улучшения

### Функциональность
- [ ] Несколько преподавателей с разным поведением
- [ ] Система энергии студента (устаёт от долгого списывания)
- [ ] Мини-игры (классика на переменах)
- [ ] Система репутации

### Графика
- [ ] Спрайты персонажей
- [ ] Анимация движения
- [ ] Партикулы при поимке
- [ ] Интерфейс в стиле 90-х

### Звук
- [ ] Фоновая музыка
- [ ] Звефекты действий
- [ ] Голосовые ремарки

### Геймплей
- [ ] Уровни сложности
- [ ] Таблица лидеров
- [ ] Достижения
- [ ] Сохранение прогресса
